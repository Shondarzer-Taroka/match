
let text = "The rain in SPAIN stays mainly in the plain";
// let match= text.match("ain");
// let match= text.match(/ain/);
// let match= text.match(/n/g);
let match= text.match(/n/gi);

//  for (const m of match) {
//     console.log(m);
//  }

//   for (const key in match) {
//      console.log(key);
//   }
 


//  console.log(typeof match);






console.log(match);